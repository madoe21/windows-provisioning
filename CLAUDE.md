# Project Operating Rules

@AGENTS.md

This repo's full operating rules live in `AGENTS.md` (the line above makes Claude Code load it
as if it were inline — see [Claude Code memory imports]). `AGENTS.md` is agent-agnostic: it's
also what OpenAI Codex CLI reads by convention, and what `.github/copilot-instructions.md` points
GitHub Copilot to. Edit `AGENTS.md`, not this file — this file only exists so Claude Code's
default `CLAUDE.md` lookup finds something.

[Claude Code memory imports]: https://docs.claude.com/en/docs/claude-code/memory
