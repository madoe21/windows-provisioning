---
description: Learn an existing codebase and persist it into project memory + CLAUDE.md + arc42.
---

Use the **onboarder** agent. Study this existing project (graphify + reading) and persist what you
learn so future sessions start informed: write `.claude/memory/codebase-map.md` and
`conventions.md` (and index them in `.claude/MEMORY.md`), fill the `[EDIT ME]` blocks in
`CLAUDE.md §1`/`§2`, and fill the sections of `docs/architecture/arc42.md` you can establish from
the code. Verify facts (e.g. the real build/test commands) before writing; mark inferences as such.

Edit only memory, CLAUDE.md, and docs/ — never application code, tests, configs, or issues. Finish
with a summary of what was learned and which files changed.
